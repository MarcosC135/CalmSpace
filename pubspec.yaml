name: calm_space
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Firebase
  firebase_core: ^4.6.0
  firebase_auth: ^6.3.0
  cloud_firestore: ^6.2.0
  google_sign_in: ^6.1.5

  # State management — NUEVO: necesario para HU-06
  provider: ^6.1.2
  firebase_core: ^4.6.0
  firebase_auth: ^6.3.0
  google_sign_in: ^6.1.5
  cloud_firestore: ^6.2.0
  google_fonts: ^8.0.2
  flutter_svg: ^2.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.4.4
  build_runner: ^2.4.10
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true